import { Route } from "@angular/router";
import { InstallComponent } from "./install.component";


export const installRoutes: Route[] = [
    {
        path     : '',
        component: InstallComponent,
    }
]